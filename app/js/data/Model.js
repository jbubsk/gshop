/*function User (user){
    this.firstName = user.firstName || '';
    this.lastName = user.lastName || '';
    this.age = user.age || 0;
    this.accounts = user.accounts || [];
    this.balance = user.balance || 0;
}

function Account(account){
    this.type = account.type || 'checking';
    this.accNumber = account.accNumber || '23333545654000012343';
    this.cards = account.cards || [];
    this.dailyLimit = account.dailyLimit || 15000;
    this.monthlyLimit = account.monthlyLimit || 250000;
}

function Card(card){
    this.cardNumber = card.cardNumber || '1234123412341234';
    this.panNumber = card.panNumber || '**** **** **** 1234';
    this.cardHolder = card.cardHolder || '';
    this.dateTo = card.dateTo;
}*/

function Product(){

}

module.exports = Model;